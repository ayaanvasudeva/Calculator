# WIDIGTS  There is a Button() , Label(),Tk(), grid(), Entry()

# BUTTON  for button() you can use padx and pady. You can use command and change button colour using fg and background coulur using bg. To change coulur you can use hex coulur codes. You can do this  mybutton['state'] = DISABLED

# INPUTS   If you want to use inputs you use the Entry widigt. You can change the width by using width = 20 .Change the border by doing borderwidth = 5.

# FONT for example do font = ("arial", 20)

# SCREEN SIZE for exaple root.geometry("600x600")

#Label for example you can remove label like this mylabel.destroy()

#TITLE to change title you do this root.title("random title")

# you can make a variable global like this: global myVariable
# you can do this columnspan = 3
# You do this entry1.delete(0, END)
              # entry1.insert(0, number)

#text = entry1.get()
  #hi  = Label(root, text = text).grid(row = 1, column = 1)
  #return None